import { CompareTextPipe } from './compare-text.pipe';

describe('CompareTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CompareTextPipe();
    expect(pipe).toBeTruthy();
  });
});
